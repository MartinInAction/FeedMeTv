import * as ApiHandler from '../libs/ApiHandler'

export let searchSubtitle = (query: string) => ApiHandler.searchSubtitle(query)

export let searchQuote = (query: string) => ApiHandler.searchQuote(query)
